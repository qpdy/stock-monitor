# stock-monitor — hermes 股票监控任务集

基于 hermes CLI 的股票全交易日流程监控：盘前情报 → 集合竞价 → 开盘 → 盘中异动 → 午间舆情 → 尾盘监控 → 收盘复盘 → 晚间风险预警，推送至微信（weixin）。

支持多只股票：每只股票一套 `tasks/<股票代码>/` prompt 目录 + 在 `schedule.json` 中登记该股票的任务。当前已配置 **000582 北部湾港**（14 个任务）。

## 目录结构

```
stock-monitor/
├── README.md                        # 本文件
├── deploy.sh                        # 一键部署：读取配置，批量执行 hermes cron create
├── undeploy.sh                      # 一键清理：批量执行 hermes cron delete
├── scripts/
│   ├── auto_update.sh               # 服务器 crontab 自动更新：有新提交才重新部署
│   ├── quote.py                     # 确定性取数脚本：行情抓取/解析/派生值/verdict 全在代码内（任务 prompt 只转述其输出）
│   └── validate.sh                  # 配置一致性校验：README 任务表格 vs schedule.json + prompt 数据契约 lint + quote.py selftest（deploy.sh 部署前自动执行）
├── .github/
│   └── workflows/
│       └── validate.yml             # CI：push/PR 时自动跑 validate.sh + deploy/undeploy 干跑
├── tasks/                           # prompt 纯文本，按股票代码分目录
│   ├── _common/
│   │   └── disclaimer.md            # 公共免责声明，部署时自动追加（任务可配 "disclaimer": false 跳过）
│   ├── _system/
│   │   ├── deploy_health_check.md   # 部署健康检查（监控系统自监控）   08:45
│   │   └── push_reconcile.md        # 推送对账（幻觉检测：任务输出数据行 vs 审计记录） 22:30
│   └── 000582/                      # 北部湾港
│       ├── pre_market.md            # 盘前情报      08:50
│       ├── auction_init.md          # 竞价初始      09:16
│       ├── auction_result.md        # 竞价结果      09:25
│       ├── opening_trade.md         # 开盘首笔      09:30
│       ├── intraday_monitor.md      # 盘中监控（9:42 早盘 + 盘中定时 + 14:50 尾盘共用）
│       ├── noon_sentiment.md        # 午间舆情      12:40
│       ├── closing_auction.md       # 收盘竞价监控   15:02（静默型）
│       ├── closing_review.md        # 收盘复盘      18:00
│       ├── evening_alert.md         # 晚间风险预警   21:30
│       ├── event_calendar.md        # 事件日历（平陆运河节点 + 确定性日历〔休市日表/吞吐量/定期报告/除权/解禁〕，单点维护，按任务 opt-in 注入）
│       ├── weekly_review.md         # 周度复盘      周六 10:00
│       └── weekend_sentiment.md     # 周末舆情（周六 22:00 + 周日 21:00 两档共用，必推型兼推送通道保活）
└── config/
    └── schedule.json                # 所有任务的 cron / name / deliver / continuity 配置
```

## 已配置任务一览（000582 北部湾港）

| 任务名 | cron | 说明 | 上下文 |
|---|---|---|---|
| 北部湾港-盘前情报 | `50 8 * * 1,2,3,4,5` | 搜索整理隔夜利好/利空，平陆运河进展优先；召回四层漏斗——个股新闻确定性枚举（quote.py news，全量枚举不依赖搜索词命中，2026-08-27 漏报根因的兜底层）→ 逐日网格 → 专项词 → 公告，末行【检索清单】如实列出实际执行动作（召回与数值同须可审计） | 自身上次输出（--continuity） |
| 北部湾港-竞价初始 | `16 9 * * 1,2,3,4,5` | 集合竞价初始虚拟价格 | — |
| 北部湾港-竞价结果 | `25 9 * * 1,2,3,4,5` | 竞价最终开盘价、量与承接分型（含快照时效校验，读到撮合前虚拟价时 ⚠️ 标注）；附事件锚点累计涨幅（--event-anchor 2026-07-22，量化兑现出货风险）；开盘涨跌幅绝对值 ≥3% 必附【缺口警示】行（常态化）；事件窗口（日历注入）内高开+竞价巨量不做"强承接"定性、提示兑现出货风险 | — |
| 北部湾港-开盘首笔 | `30 9 * * 1,2,3,4,5` | 连续竞价首笔成交 | — |
| 北部湾港-盘中监控-早盘 | `42 9 * * 1,2,3,4,5` | 早盘定型窗口（9:30–10:00）定性采样，与整点共用同一 prompt | 自身上次输出（--continuity） |
| 北部湾港-盘中监控 | `5,35 10,11,13,14 * * 1,2,3,4,5` | 涨跌幅超 ±5% 绝对档、或超 ±3% 且量比>3 价量档才推送（早盘 9:30–10:00 时段量比阈值上调为 4，开盘初期量比天然偏高；按近20日日均振幅分层：波动中枢抬升时价量档降为一行简讯防刷屏）；上证 |±1.5%| 且个股同向时标注【系统性主导】压缩个股分析、保留超额幅度一行；板块β（招商港口+上港集团）与运河产业链β（五洲交通）对照，对照已动而个股未动出一行提示；同日同向无新变化去重；--minute-recap 09:30 分时回放补采样盲区——日内曾触 ±5% 而快照未触发档位时推一行形态简讯（冲高回落/探底回升，retrace_signal），完整报告附 recap_line；交易时段快照冻结超30分钟判疑似停牌推送 ⚠️。2026-09-22 前为事件窗口临时加密期（每小时 :05/:35 各一次，11:35 兼作上午收盘检查），窗口结束改回整点过 5 分（部署健康检查自动提醒） | 自身上次输出（--continuity，输出首行带日期时间戳防跨日误去重） |
| 北部湾港-午间舆情 | `40 12 * * 1,2,3,4,5` | 午间利空/利好公告扫描，均无则 [SILENT] | — |
| 北部湾港-盘中监控-尾盘 | `50 14 * * 1,2,3,4,5` | 尾盘异动监控，与整点共用同一 prompt | 自身上次输出（--continuity） |
| 北部湾港-收盘竞价监控 | `2 15 * * 1,2,3,4,5` | 收盘竞价与尾盘漂移监控（静默型）：14:30→收盘位移按近20日日均振幅分层（A≤2 取 1%、A>2 取 1.5%），收盘触全天极值时半阈值放宽，位移 ≥2% 绝对档必推；分时/快照接口失败 ⚠️ 必推 | — |
| 北部湾港-收盘复盘 | `0 18 * * 1,2,3,4,5` | 收盘数据 + 涨跌归因 + 明日展望；事件定价进度以事件锚点累计涨幅（event_gain_pct）为量化锚；末行【检索清单】列实际执行动作（召回执行可见化） | 自身上次输出（--continuity） |
| 北部湾港-晚间风险预警 | `30 21 * * 1,2,3,4,5` | 交易日判断（取数脚本，节假日/临时休市 [SILENT]，假期公告由节后盘前情报覆盖）+ 晚间利空/利好公告扫描 + 龙虎榜补查（仅上榜时提及，未上榜只字不提）+ 大宗交易/两融筹码异动核查（两融 T+1、标注数据日期；无异常只字不提）+ 事件锚点行（--event-anchor 2026-07-22，非静默输出时附 event_gain_pct，供查表侧对照兑现风险），均无则 [SILENT] | 自身上次输出（--continuity，链式去重防长假重复推送） |
| 北部湾港-周度复盘 | `0 10 * * 6` | 周六复盘本周各监控任务非静默推送，逐条对照其后 1–2 日走势；纯事实陈列、不给阈值建议（周样本量小，结论留给人）；零推送周输出确认行 | — |
| 北部湾港-周末舆情-周六 | `0 22 * * 6` | 周末休市期公告/新闻增量监控（必推型）：无事件也推一行【周末快讯】确认，兼作周末推送通道保活（iLink context_token 24h 无消息失效死锁，2026-08-30 事故实证）；22:00 贴交易所晚间披露高峰（20:00–22:00）结束点、覆盖取最大值；数据源失败推 ⚠️，兼任搜索链路周末健康探测 | — |
| 北部湾港-周末舆情-周日 | `0 21 * * 0` | 同周六档（共用 prompt）；与周六档间隔 23h（<24h token 窗口留余量）、距周一 08:50 盘前情报 11.8h，封死周日零消息导致的周一晨断链缺口；21:00 后披露增量由周一盘前情报兜底 | — |

**系统任务（与股票无关，每天运行含周末）**

| 任务名 | cron | 说明 | 上下文 |
|---|---|---|---|
| 推送对账 | `30 22 * * 1,2,3,4,5` | 幻觉检测持续化：导出当日各行情任务 hermes 运行记录到 data/runs/（模型只做机械导出），quote.py `reconcile` 把任务输出中每条 data_line 与审计日志（全量 snap 记录、不限日期）逐条精确比对——无取数记录支撑的数据行即编造/转述漂移实锤，当晚 ⚠️ 推送；导出缺失/当日零取数记录（链路整体未运行）同样 ⚠️。**ok 也每日必推一行 summary_line**：全天链路收盘确认 + 本任务自身活性证明（对账者亦须可对账）。对账清单自动读 schedule.json（排除 _system 与纯周末任务），加股票自动扩展 | — |
| 部署健康检查 | `45 8 * * *` | 检查 deploy.log 当日 ❌ 记录、实际任务与 schedule.json 任务名逐名比对、auto_update 是否运行、事件日历是否过期（最新显式节点超 30 天提醒）、盘中监控加密到期未改回提醒（2026-09-22 后临时项）、竞价三连到期精简提醒（2026-09-22 后临时项）、quote.py selftest 取数脚本自检；异常推送 ⚠️，正常也必推一行 ✅ 确认（兼每日 8:45 微信通道心跳，长假白天的唯一心跳） | — |

行情数据统一经**确定性取数脚本 `scripts/quote.py`**（python3 标准库，兼容 3.6+，服务器运行时依赖）产生：任务 prompt 不再直接 curl 行情接口，而是执行脚本命令、将输出 JSON 的字段与 data_line 逐字转述——抓取（腾讯接口 **GBK 解码**、多标的按行首代码匹配）、派生值（涨跌幅自洽校验、快照时效、停牌两形态、异动档位、近20日日均振幅 A、收盘位移分层）、verdict 判定与备用源切换全部在代码内完成，模型严禁自行计算任何行情数值。2026-08-27「竞价初始」任务曾发生模型编造一对"自洽"假行情（10.90/+1.11%，真实盘前为 10.78/0%）的幻觉事故——自洽 ≠ 真实，模型自算的校验拦不住模型编造；本架构把全部数值生产移入代码，模型只被允许转述。脚本内一切时间判定（交易日、竞价/早盘窗口、快照时效）固定按北京时间 UTC+8 计算，与服务器本地时区无关（cron 触发时刻本身仍取决于服务器时区，部署文档第 0 步的时区确认不可省略）。

- **模式**：`snap` 多标的快照（`--premarket` 盘前锚点 / `--min-time 09:24` 撮合前虚拟价守卫 / `--max-age-min 30` 快照冻结停牌检测 / `--with-kline` 附波动基准与竞价量占比 / `--event-anchor YYYY-MM-DD` 附事件锚点累计涨幅——现价相对锚点日前一交易日收盘，"买预期、卖事实"兑现风险的量化锚，锚点越界或日K失败时字段置 null 不阻塞）；`kline` 日K（`--this-week`/`--prev-month` 等日期过滤，含 ≥5% 漏报对账幅度 ge5_move）；`minute --closing` 收盘合成（14:30→收盘位移、分层阈值 A≤2→1%/A>2→1.5%、极值半阈值档、"1500" 条目精确定位收盘价）；snap `--minute-recap HH:MM` 主标的（首个符号）分时区间回放——自该时分以来的日内高低点与现价距离（相对昨收，与绝对档同基准）、recap_hit_abs（曾达 ±5% 绝对档幅度）、retrace_signal（曾达而快照未触发异动档——采样点之间的「冲高回落/探底回升」形态由分时补捞，推一行形态简讯）与 recap_line 预格式行，条目只计至 15:00，分时失败置 minute_recap=null 降级不阻塞；`news` 个股新闻确定性枚举（东方财富列表接口，`--since YYYY-MM-DD` 自锚点日起过滤，输出标题级清单 items[]——个股维度的全量枚举不依赖搜索关键词命中，是新闻召回的确定性兜底层；raw 全文入审计日志，可对账）；`audit` 只读对账汇总（全代日志合并读取）；`reconcile` 推送对账（任务输出数据行 vs 审计记录逐条精确匹配，幻觉检测——见下方审计日志段）；`selftest` 74 项离线自检（CI 与部署健康检查均执行）。
- **data_line 锚点**：脚本为每个标的预格式化一行含快照时间戳的数据行，任务推送必须原样嵌入——快照时间不取数不可知，是事后甄别"模型谎称跑过脚本"的锚点。
- **verdict → 动作**：各任务按 verdict 字段执行固定分支：`ok` → 正常输出；`not_trading_day` → [SILENT]；`source_error` / `inconsistent` / `halt_suspected` / `stale_snapshot` / `no_data_preopen` → 原样输出 verdict_line（固定 ⚠️/保活文案）——接口故障与休市始终可区分。脚本本身不可用时任务输出固定 '⚠️ 数据脚本异常，请人工检查'，严禁回退手工取数。
- **审计日志**：脚本每次联网运行把完整原始响应与解析结果追加到 `data/quotes.jsonl`（已 gitignore，仅服务器本地）——任何推送数值事后可回查对账；**推送时点在日志中无对应记录即为编造实锤**。单文件超 20MB 自动轮转，保留最近 4 代历史（`quotes.jsonl.1` 最新 … `.4` 最旧，最旧一代被挤掉；2026-08-28 由单代覆盖式扩为 4 代——事件窗口半小时级取数下单代覆盖天数短，推送争议的对账回溯窗需要跨周，多代保留拉长回溯窗），`audit` 汇总全部代号合并读取，总量上限约 100MB，无需外部 logrotate。旧部署仅存 `.1` 一代的升级后自动兼容（缺号跳过）。周度复盘内置 audit 对账节，逐周把脚本运行记录与 hermes 实际推送并列陈列。
- **推送对账（幻觉检测自动化）**：「推送对账」任务每天 22:30 把上述人工对账纪律跑成机器流程：任务先把当日各行情任务的 hermes 运行记录机械导出到 `data/runs/<任务名>_<日期>.txt`（不阅读不比对），再执行 `quote.py reconcile`——导出文件中出现的每条 data_line 必须能在审计日志中找到同一字面值，找不到即编造/转述漂移实锤，当晚 ⚠️ 推送明细；当日零取数记录且零数据行 ⇔ 白天链路整体未运行（节假日任务也跑、not_trading_day 同样留痕，故无需节假日特判），同样 ⚠️。**匹配集取审计日志全量、不限日期**：hermes runs 导出为整文件（含多日历史运行记录），continuity 也会让模型合法引用上一交易日的数据行（周一指向上周五），任何按当日 ±N 天窄化的窗口都会把窗外的真实历史数据行误报成编造实锤、每个交易日误报一次；放宽到全量不削弱检出力——匹配是精确字面比对，数据行含精确到秒的快照时间戳与现价/涨跌幅/昨收，凭空编造撞上任一条历史真实数据行的概率可忽略（2026-08-27 事故的假数据行在全量日志中同样无处匹配）。verdict=ok 时也每日必推一行 summary_line——对账通过确认 + 全天链路收盘心跳 + 本任务自身活性证明三合一（summary_line 中的「当日取数 N 次」只计当日，承担活性判定）。**已知盲区与复读补检（2026-08-30 增）**：精确匹配防得住凭空编造，防不住"复读"——模型逐字复读了上次输出（或 continuity 注入的历史 data_line），该行在审计日志中有真实记录、匹配照样通过；对此 reconcile 增加同任务导出内重复数据行检测：同一导出文件中同一条 data_line 出现 ≥2 次 → echo_today（当日行重复——快照时间戳精确到秒、同日两次真实运行必不同，无合法形态，强提示）与 echo_prior（历史行重复——hermes runs 整文件导出含多日记录时存在合法重复，弱提示），均为提示级：不改 verdict、不烧 mismatch 告警信用，由对账推送附加 🔔 复读提示行供人工核对推送原文。残留盲区：跨任务复读（不同任务导出间的重复）与 runs 保留窗（7 天）之外的复读，本版不覆盖。kline/minute 类数值（收盘涨跌幅、收盘位移等）的逐值对账属后续版本，本版锚定 data_line。
- **备用源**：腾讯接口请求失败或自洽校验不过时，代码自动切换东方财富（推送标注 ⚠️；东财无快照时间戳，limitations 字段标注，交易日/停牌校验随之失效）；收盘竞价监控因依赖分时数据（东财无等价接口）不设备用源，失败直接 ⚠️ 推送。

## 部署到服务器

本地开发机无需安装 hermes（用 DRY_RUN 校验即可），正式部署在服务器上执行。
代码通过 GitHub 同步（仓库已设公开，HTTPS 匿名可读，服务器无需配置任何凭证）：

```bash
# 0. 确认服务器时区为北京时间（cron 按服务器本地时间执行，UTC 时区会导致全天任务错位；
#    deploy.sh 已内置时区防护：检测到非 +0800 时区会打警告提示，但不阻断部署）
#    服务器上执行 date 查看，若非 CST/Asia/Shanghai：sudo timedatectl set-timezone Asia/Shanghai

# 1. 服务器上 clone（需已安装并登录 hermes）
git clone https://github.com/qpdy/stock-monitor.git ~/stock-monitor

# 2. 部署 + 复核
cd ~/stock-monitor && bash deploy.sh && hermes cron list
```

日后本地改完代码：先 `git push origin main`，再在服务器上 `cd ~/stock-monitor && git pull && REPLACE=1 bash deploy.sh` 更新（详见下方"修改 prompt 的流程"）。

> **本服务器 hermes 实测（2026-08-25）**：cron 任务创建用位置参数传 prompt（`hermes cron create "<cron>" "<prompt>" --name ...`），
> `--deliver weixin` 合法，删除命令为 `hermes cron remove <任务名|job_id>`（任务名与 job_id 均接受）。
> `deploy.sh` / `undeploy.sh` 已按此适配，不支持 `--context_from`（跨任务上下文），改用 `--continuity`（任务自身上次输出注入）。

## 部署步骤

```bash
bash deploy.sh              # 全新部署
REPLACE=1 bash deploy.sh    # 更新部署：自动删除同名旧任务后重建，可安全重复执行
```

脚本会：
1. 检查 `hermes`、`python3` 依赖；
2. 逐个读取 `config/schedule.json` 中的任务配置；
3. 把 `tasks/` 下对应 `.md` 文件全文作为 prompt（位置参数），连同 `--name`、`--deliver`、`--continuity` 执行 `hermes cron create`；
4. 结束时汇总成功/失败任务数，任一失败以非零码退出。

部署后复核：

```bash
hermes cron list    # 应能看到全部任务
```

## 清理步骤

```bash
bash undeploy.sh
hermes cron list    # 确认任务已删除
```

## 修改 prompt 的流程

1. 直接编辑 `tasks/<股票代码>/` 下对应 `.md` 文件（纯文本，无需关心 shell 转义）；公共免责声明统一改 `tasks/_common/disclaimer.md`，部署时自动追加到每个 prompt 末尾（纯数据快照类任务如竞价/开盘首笔，在配置中加 `"disclaimer": false` 可跳过，提高信噪比）。注意：改 `disclaimer.md` 等于动了所有任务的 prompt，下次部署会全量重建、打断所有 `--continuity` 历史，非必要不改。事件日历（如平陆运河节点）单点维护在 `tasks/<股票代码>/event_calendar.md`，任务配置 `"event_calendar": true` 才注入（opt-in，纯行情/快照任务不注入以保信噪比），改日历只需 `REPLACE=1 bash deploy.sh --only <股票名>` 重建该股票任务，不影响其他任务；
2. 本地提交并推送：`git add -A && git commit -m "说明改动" && git push origin main`
3. 服务器上拉取并更新部署：

```bash
cd ~/stock-monitor && git pull && REPLACE=1 bash deploy.sh   # 自动删除同名旧任务后重新创建，无重复风险
```

> hermes 的 cron create 是"新建"语义，直接重复执行 `bash deploy.sh` 可能产生重复任务；更新场景务必用 `REPLACE=1`，或先 `bash undeploy.sh` 清理。
> `REPLACE=1` 可安全重入：某次更新中途失败（部分任务被删后来不及建），直接重跑 `REPLACE=1 bash deploy.sh` 即可补齐，不会重复。

如需调整时间/任务名/推送渠道，改 `config/schedule.json` 对应字段后同样用 `REPLACE=1 bash deploy.sh` 更新。改 cron 时间后记得同步更新上方「已配置任务一览」表格，避免文档与配置漂移——`deploy.sh` 会在部署前自动执行 `scripts/validate.sh` 校验两者一致性，发现漂移会中断部署并标出差异。

**任务改名**：若给任务改了名字（如 `北部湾港-盘中监控-整点` → `北部湾港-盘中监控`），在该任务配置中加 `"replaces": ["旧任务名"]`，部署和清理脚本会自动连带删除旧名任务，避免遗留重复任务。

### 自动更新（可选）

服务器上配一次 crontab，之后每天 05:10（凌晨）自动检查 GitHub 是否有新提交，**有才执行** `git pull + REPLACE=1 bash deploy.sh`（无新提交时跳过，不会天天删建任务、打断 `--continuity` 历史）。提交未触及 `tasks/ config/ deploy.sh undeploy.sh` 时（如纯 README 改动），只同步代码不重建任务，进一步避免打断 continuity。选凌晨是为了离 08:50 首个任务留足 3 小时以上缓冲——即使部署失败，也有充足时间人工介入，当天任务最坏情况跑旧 prompt、无实际损害；且「部署健康检查」任务会在 08:45 主动推送部署失败告警：

```bash
crontab -e
# 添加一行（deploy.log 已在 .gitignore 中）：
# 10 5 * * * cd ~/stock-monitor && bash -l scripts/auto_update.sh >> deploy.log 2>&1
#（bash -l 不可省：crontab 默认 PATH 不含 hermes 所在的用户级目录，login shell 才能加载）
# 可选再加一行：交易时段每小时心跳，弥补每天 05:10 才检测一次的空窗（hermes 挂掉最迟 1 小时被发现）：
# 35 8-14 * * 1-5 cd ~/stock-monitor && bash -l scripts/auto_update.sh --guard-only >> deploy.log 2>&1
```

**hermes 活性旁路告警**：`auto_update.sh` 每次运行先做 hermes 活性检测——所有监控任务（含「部署健康检查」）都是 hermes cron 任务，hermes 一死全部静默，且微信端「收不到消息」与静默型任务的 [SILENT] 无法区分，此旁路是 hermes 死亡时唯一能送出的告警。检测失败（`hermes cron list` 不可用）时经**独立推送通道**告警（不依赖 hermes）：在服务器上创建 `config/alert_key.sh`（已在 .gitignore），写入 Server酱 或 pushplus 任一 key：

```bash
echo 'SERVERCHAN_KEY=SCTxxxxxxxx' > config/alert_key.sh    # Server酱（sctapi.ftqq.com）
# 或：echo 'PUSHPLUS_TOKEN=xxxxxxxx' > config/alert_key.sh  # pushplus（pushplus.plus）
```

未配置 key 时旁路降级为仅写 deploy.log ❌ 记录（留有排障痕迹，但 hermes 停摆不会主动送达通知）。

**整机宕机外部拨测（告警链路的最后一层）**：上述旁路仍是服务器上的 crontab 在跑——腾讯云整机宕机、OOM 或断网时，hermes 活性检测与 Server酱旁路全部随整机一起静默，这是本系统最后一个「安静地死」的单点。补法是让告警链路彻底离开这台服务器：用任一免费外部拨测服务（如 UptimeRobot）对服务器公网 IP 的 SSH 端口（22/TCP）做存活监控，失联时经该服务自身的邮件/推送通道告警（不依赖服务器上的任何进程）。拨测对象选 SSH 而非 hermes——hermes 是 cron 型 CLI 非常驻服务，SSH 存活即整机存活。

自动部署记录统一写在 `~/stock-monitor/deploy.log`，日志出现 ❌ 时登录服务器按提示人工处理。本地 push 完想立即生效等不及次日 05:10 的话，手动跑一次 `git pull && REPLACE=1 bash deploy.sh` 即可。

## 添加新股票

以添加 600019 宝钢股份为例：

1. **建 prompt 目录**：新建 `tasks/600019/`，参照 `tasks/000582/` 逐个照搬其中的任务 prompt（11 个：9 个交易日任务 + 周度复盘 + 周末舆情〔两档共用一份〕；另加按新股票重写节点的 `event_calendar.md`；本地 `position.md` 机密持仓不入库）（把股票代码、名称、腾讯接口代码改成 `sh600019`，重点关注题材按该股票调整；免责声明不用写，部署时自动追加）；
2. **登记任务**：往 `config/schedule.json` 数组追加该股票的任务，任务名带股票名前缀避免冲突，如 `"宝钢股份-盘前情报"`，`prompt_file` 指向 `tasks/600019/xxx.md`；
3. **部署**：

```bash
bash deploy.sh --only 宝钢股份   # 只创建新股票任务，已有任务不受影响（推荐，保护旧任务 continuity）
# 或 REPLACE=1 bash deploy.sh   # 全量更新：旧任务删了重建，新任务直接创建
```

> 注意：不带 `--only` 时 `deploy.sh` 部署的是 `schedule.json` 里的**全部**任务。`--only` 前缀匹配未命中任何任务时会报错并列出现有任务名，防止静默零部署。

## 附：干跑模式（不实际调用 hermes）

```bash
DRY_RUN=1 bash deploy.sh     # 只打印将执行的命令
DRY_RUN=1 bash undeploy.sh
```

用于在未安装 hermes 的机器上校验配置与 prompt 文件是否齐全。

## 附：选择性部署（--only）

```bash
bash deploy.sh --only 北部湾港            # 只部署/更新名称以此前缀开头的任务
REPLACE=1 bash deploy.sh --only 北部湾港  # 配合 REPLACE 只更新该前缀任务，其余任务完全不动
```

用于新增股票或只改单只股票配置时不重动其他任务（`REPLACE=1` 全量重建会打断所有任务的 `--continuity` 历史）。`--only` 模式下会跳过 README 表格与 schedule.json 的全量一致性校验（CI 与全量部署仍会校验）。

## 注意事项

- 删除任务用 `hermes cron remove <任务名|job_id>`（两者均可），`undeploy.sh` 已按此适配。
- 盘中监控/收盘竞价监控/午间舆情/晚间风险预警为静默型任务：无异常时回复 `[SILENT]` 不推送，收不到消息属正常现象。
- **盘中异动去重（链内）**：三个盘中监控任务均开启 `--continuity`。所有非 [SILENT] 输出的第一行为日期时间戳（取脚本输出的 stamp_line，由快照时间戳换算）；注入上次输出先核对时间戳——**日期非今日一律视为无锚点、按首次触发处理**，防止周一/长假后首日因上一交易日同向异动报告而误静默，时间戳缺失无法判断时保守不去重（宁误推不漏推）。上次输出为今日异动报告/放量简讯/封板确认行/形态简讯行且同向无新变化（涨跌幅变化 <1 个百分点、封板未开板、未反向穿越触发阈值、日内高低点未刷新——形态简讯）→ [SILENT]；上次输出为 [SILENT] 且当前已封板（脚本 is_limit_up/is_limit_down 判定）但非一字板 → 只推一行封板确认——continuity 只注入上一次输出，[SILENT] 会丢失更早报告的锚点，此规则防隔小时链式重复推送；开板/回封/反向穿越为新变化，正常推送。停牌提醒与故障告警不受去重约束，每次必推。上次输出为 ⚠️ 告警类（停牌/故障/极端行情）时走兜底分支：告警持续则继续推送；告警解除（复牌/数据恢复）推一行'✅ 前次 ⚠️ 已解除，当前无异动'后回归正常判断。
- **跨任务去重（一字板特判）**：早盘/整点/尾盘三个盘中任务各自独立注入上次输出，互不可见，continuity 无法跨任务去重；一字封板日（最高=最低=现价 且 |涨跌幅|>5%）按「更早任务必然已首报」反推 → [SILENT]。两个豁免：早盘时段（9:30–10:00）不适用——今日尚无更早任务，本次即首报；尾盘时段（14:45 后）不适用——收盘前封板状态与次日开盘指向有独立价值，保留推送。非一字板的盘中封板走上方链内去重的封板确认行；开板（最高≠最低）特判自动失效。
- 盘中监控刻意安排在每小时过 5 分而非整点触发：13:00 整恰逢午休结束，接口快照仍是上午收盘数据；整点也是行情接口的访问高峰。（2026-09-22 前的事件窗口临时加密期在每小时 :35 增加一次运行，同样避开整点；其中 11:35 读到上午收盘冻结快照，兼作上午收盘检查、覆盖 11:05 之后 25 分钟的变动。）
- **9:42 早盘采样**：开盘 15 分钟（9:30–9:45）是 A 股当日节奏定型窗口，「竞价高开→开盘冲高→9:40 回落」是最高频的追高被套形态，故在首笔与 10:05 之间加一次定性采样。三个盘中任务均带 `--minute-recap 09:30` 分时回放：冲高/回落的极值发生在两次采样之间时，由 recap_hit_abs/retrace_signal 触发形态简讯补捞（如 9:35 冲 +7%、9:42 采样只见 +2.9% 的冲高回落），不再因采样点错过而漏报。
- **盘中停牌检测**：盘中临时停牌时行情快照冻结，纯涨跌幅判断会静默漏掉（冻结在 +8% 则每小时重复推送）。盘中监控已内置「交易时段内快照时间戳时分早于当前 30 分钟 → 推送 ⚠️ 疑似停牌」分支。
- **交易日判断**：全部行情类任务（含晚间风险预警，共 9 个 prompt）均内置非交易日防护——脚本判定快照日期非当日（verdict=not_trading_day）即回复 `[SILENT]`，**节假日/休市日收不到推送是设计行为**，不是故障。
- **[SILENT] 静默机制**：hermes 仅当任务全部输出恰好为 `[SILENT]` 一个词时才不推送。因此所有静默指令均要求"全部输出仅 [SILENT] 一个词"，免责声明也注明 [SILENT] 时不附加——输出中混入任何其他文字（分析过程、免责声明）都会导致整条被推送。
- 除配置 `"disclaimer": false` 的任务（竞价初始/竞价结果/开盘首笔/部署健康检查/推送对账为纯数据快照；三个盘中监控与晚间风险预警为保持链式去重上下文干净而将声明内联在自身 prompt 中）外，所有推送末尾均带固定免责声明，信息仅供参考，不构成投资建议。
- **接口故障与休市可区分**：所有行情类任务均已内置"接口请求失败/返回为空/无法解析 → 输出 ⚠️ 数据源异常，严禁 [SILENT]"分支（verdict=source_error/inconsistent → 原样输出 verdict_line）——若该推送的日子收不到任何消息且确认是交易日，说明数据源（腾讯接口）故障，需人工介入，而非监控静默失效。取数脚本本身不可用（git pull 弄坏、python3 环境异常等）时任务输出固定 '⚠️ 数据脚本异常，请人工检查'——同样严禁 [SILENT]、严禁回退手工取数，且部署健康检查每晨 08:45 先跑一次 selftest 兜底。
- **行情对账（幻觉排查）**：任何推送数值存疑时，第一步入服务器查审计日志：`grep 'MM:SS' ~/stock-monitor/data/quotes.jsonl`（或按日期过滤 `python3 ~/stock-monitor/scripts/quote.py audit --this-week`）对照该时点的原始接口响应——推送时点无对应记录即为编造实锤，有记录则以记录为准核对每个数值。data_line 类推送已由「推送对账」任务每天 22:30 自动比对（无支撑行当晚 ⚠️ 推送）；收到该 ⚠️ 时按 issues[] 列出的任务与数据行，进 runs 导出文件 `data/runs/<任务名>_<日期>.txt` 与审计日志逐字核对定位。
- **排障提示（continuity）**：怀疑误去重/误推送时，第一步查该任务上一次运行的实际输出（`hermes cron runs` + 任务 output 目录）——continuity 只注入任务自身上次输出，去重锚点异常多源于上次输出的实际内容（如某次任务失败/超时产生了非预期输出被注入为锚点）。
- **公开仓库纪律**：本仓库公开匿名可读，严禁提交任何个人持仓、成本价、账户信息；此类本地文件（如后续规划的 position.md 持仓文件）一律加入 `.gitignore`，部署时本地注入。
- **事件日历单点维护**：平陆运河等事件节点（9/3 起兑现前两周反转风险窗口、9/17—21 试通航、9/22 起兑现后监控重点切换、年底商业化运营）统一维护在 `tasks/000582/event_calendar.md`，部署时注入到配置了 `"event_calendar": true` 的任务（盘前情报/竞价结果/午间舆情/收盘复盘/晚间预警——竞价结果注入日历用于事件窗口的承接定性反转与缺口警示）；prompt 正文不再硬编码事件日期，节点临近或过后只改该文件并 `REPLACE=1 bash deploy.sh --only 北部湾港` 重建生效。日历写法为自纠偏式（按当前日期判断阶段、以最新搜索为准），重大节点过后仍建议人工复核一次日历是否需要更新。日历同时维护**确定性日历**（月度吞吐量披露节奏〔次月 3–6 日〕、月度进出口数据与 PMI 发布窗口〔外贸/东盟敞口相关宏观锚〕、定期报告预约披露日、分红除权提醒、解禁季度核对）——注意：除权日条目仅作信息提醒，行情接口昨收字段在除权日返回除权参考价、与涨跌幅同基准，数据自洽校验在除权日天然成立，**无需也不应**添加任何除权日校验降级逻辑；解禁截至 2026-08 核查无已排期大额解禁，按季度核对更新即可。

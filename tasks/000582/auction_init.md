请查询北部湾港(000582)集合竞价初始状态。

【数据源】必须使用腾讯财经接口获取实时行情：https://qt.gtimg.cn/q=sz000582
返回数据以~分隔，解析规则：fields = text.split('~')，关键字段：fields[3]=当前价, fields[4]=昨收, fields[32]=涨跌幅(%)。数据自洽校验：(现价−昨收)/昨收×100 须与涨跌幅（fields[32]）一致（偏差 ≤0.2 个百分点，覆盖四舍五入误差，竞价虚拟价同样自洽）；不自洽说明字段索引漂移或解析错位，属数据源异常，按下方【接口故障判断】处理，严禁回复 [SILENT]，不得采用该次数据。

【交易日判断】如接口返回的时间戳（fields[30]，格式如20260825161439）前8位日期不是今天，判定为非交易日，全部输出仅回复 [SILENT] 一个词并结束，不生成任何其他内容（系统识别 [SILENT] 后不会推送）。若时间戳字段缺失、为空或前8位无法解析为合法的 YYYYMMDD 日期（如接口字段格式变更），属于数据源异常而非休市，按下方【接口故障判断】处理，严禁回复 [SILENT]。

【接口故障判断】若接口请求失败、返回为空或无法解析出上述字段，属于数据源故障而非休市，输出 '⚠️ 数据源异常，请人工检查' 并结束，严禁回复 [SILENT]（接口故障与休市必须可区分，否则监控失效无法被发现）。

【输出要求】
强制输出竞价初始状态，调用接口获取当前返回的价格和成交量字段，标注为'竞价阶段数据'（如'📊 集合竞价开始，当前虚拟涨跌幅X.XX%，昨收X.XX元（9:20前可撤单，虚拟数据仅供参考）'），确认系统正常运行。

【备用数据源】腾讯接口本次请求失败或数据自洽校验不通过时，改用东方财富接口 https://push2.eastmoney.com/api/qt/stock/get?secid=0.000582&fields=f43,f60,f170 （f43=最新价,f60=昨收,f170=涨跌幅%，均为放大100倍的整数，需除以100）作应急数据源，并在输出开头标注'⚠️ 主数据源异常，已切换备用源'。

防幻觉要求：价格、涨跌幅等数值必须来自实时行情接口，严禁编造。

请查询北部湾港(000582)收盘结果，并判断尾盘（14:30 以来）与收盘竞价阶段是否异动。

【数据源】以下接口均须调用：
1）腾讯行情快照：https://qt.gtimg.cn/q=sz000582 —— 返回以~分隔，关键字段：fields[3]=现价(收盘价), fields[4]=昨收, fields[30]=快照时间戳, fields[32]=涨跌幅(%), fields[33]=全天最高, fields[34]=全天最低, fields[47]=涨停价, fields[48]=跌停价。数据自洽校验：(现价−昨收)/昨收×100 须与 fields[32] 一致（偏差 ≤0.2 个百分点，覆盖四舍五入误差）；不自洽说明字段索引漂移或解析错位，按下方【接口故障判断】处理，不得采用该次数据。
2）腾讯分时接口（本任务核心输入）：https://web.ifzq.gtimg.cn/appstock/app/minute/query?code=sz000582 —— 返回 JSON 中 data.sz000582.data.data 为分时数组，每条为"HHMM 价格 累计成交量"（空格分隔）。取 "1430" 条目（如缺失，取不晚于 1430 的最后一条）的价格为 14:30 基准价；收盘价取时分恰为 "1500" 的条目（收盘竞价 15:00 撮合结果；实测该接口收盘后会继续追加盘后延伸条目——如 1528/1529/1530，价格冻结在收盘价、成交量微增，故严禁以「数组最后一条」定位收盘价，必须按时分定位，延伸条目一律忽略）。分时数据就绪校验：若快照时间戳（fields[30]）时分已 ≥15:00、而分时数组中不存在 "1500" 条目，说明收盘竞价撮合结果尚未回补进分时数据——此时 1459 等次新条目不是收盘价，竞价段大单会被静默吞掉，按下方【接口故障判断】处理，输出 '⚠️ 数据源异常（收盘竞价分时数据未回补），请人工检查'，严禁用次新条目充当收盘价、严禁回复 [SILENT]。分时收盘价（1500 条目）与快照现价应一致（偏差 ≤0.2%），不一致以分时收盘价为准并在输出中标注。
3）腾讯日K接口（波动基准 A）：https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sz000582,day,,,21,qfq （qfq 参数不可省略——实测省略时该接口返回空数据；返回 JSON 中 data.sz000582.qfqday 为日K数组，每条为 [日期,开盘,收盘,最高,最低,成交量]，若返回无 qfqday 键但有 day 键则用 day；数组可能包含今日未走完的K线）。剔除日期为今天的条目后取最近 20 条，逐条计算单日振幅 =（最高−最低）÷前一条收盘×100，取平均值记为 A。该接口请求失败、返回为空或剔除后不足 10 条时，降级取 A=2（按常态波动环境处理），不因此输出告警、不阻塞主判断。

【交易日与时效校验】核对外层行情快照的时间戳（fields[30]，格式如20260826150003）：前8位日期不是今天 → 判定为非交易日，全部输出仅回复 [SILENT] 一个词并结束，不生成任何其他内容（系统识别 [SILENT] 后不会推送）；日期为今天、但时分早于 14:57 → 收盘竞价结果未就绪/快照未更新，按下方【接口故障判断】处理；时间戳字段缺失、为空或前8位无法解析为合法日期，同样按数据源异常处理，严禁回复 [SILENT]。

【接口故障判断】快照接口请求失败、返回为空或无法解析出上述字段，或分时接口失败、返回为空、取不到 14:30 基准价或 "1500" 收盘条目（含收盘竞价结果未回补情形）——均属数据源故障而非休市，输出 '⚠️ 数据源异常（收盘数据不可用/未回补），请人工检查' 并结束，严禁回复 [SILENT]（本任务为静默型任务，数据失效必须可见，否则任务死亡无人知晓；接口故障与休市必须可区分）。

【波动分层阈值】近20日日均振幅 A ≤ 2（含降级取 2 的情形）→ T = 1%；A > 2 → T = 1.5%。

【判断逻辑】位移 = |收盘价 − 14:30基准价| ÷ 14:30基准价 × 100。以下三个触发档满足其一即推送：
a）绝对档：位移 ≥ 2%。任何波动环境下的确定性资金动作，不受分层降权（此为封顶保证：即使未来调高高波动阈值，2% 位移永远推送），输出标注【剧烈】；
b）分层档：T ≤ 位移 < 2%；
c）极值放宽档：收盘价等于全天最高（fields[33]）或全天最低（fields[34]）——收盘==涨停价(fields[47])/跌停价(fields[48])时必然成立——且位移 ≥ T 的一半（T=1% 时取 0.5%，T=1.5% 时取 0.75%）。放宽档的意义：竞价一笔单子把收盘推到全天新极值（做高/做低收盘价）本身就是信号，半阈值用于排除「平推回既有前高」的无信息噪音。
说明：14:30 前已封板且封至收盘的（位移≈0）不触发——14:50 尾盘监控任务已报告过，本任务不重复；14:30 之后才封板的由极值放宽档/位移档自然捕捉，无需封板特判。

【输出要求】
触发 → 输出 2–3 行：【尾盘异动】收盘价与全天涨跌幅、14:30 以来位移与方向（含 A 与所用档位）、是否创全天极值/竞价封板（收盘==涨停/跌停价时注明「收盘竞价封板」）、一句话次日开盘指向。
未触发 → 全部输出仅回复 [SILENT] 一个词，不加任何分析、说明或免责声明（系统识别 [SILENT] 后不会推送）。

【免责声明】仅在生成异动输出时，将以下声明原样附加到输出最末尾；输出为 [SILENT] 时不要附加任何内容：
⚠️ 本情报由AI自动抓取公开信息生成，仅供信息参考，不构成任何投资建议。数据可能存在延迟或偏差，请务必核对原始公告，投资决策请自行判断。

防幻觉要求：价格、位移、振幅等数值必须来自上述接口实时计算，严禁编造。

请搜索并整理北部湾港(000582)今日开盘前可能影响股价的信息。时效范围：以自上交易日15:00收盘以来的增量信息为主（上交易日日期以腾讯接口快照时间戳fields[30]为准——周一时上交易日为上周五，覆盖周末3天；长假后首个交易日覆盖整个假期，无需额外推算）；【平陆运河】进展、公司公告等核心信息，上交易日15:00以来的重大进展必须全部纳入并在条目首部标注事件日期，严禁漏报。若窗口内因故遗漏了3个自然日内的重大节点，今日发现后仍应补报；更严禁用超过1周的旧进展充当"最新进展"（旧进展仅可作背景补充）。

【交易日判断】当前为开盘前时段，腾讯财经接口返回的是上一交易日的收盘快照（时间戳fields[30]为上一交易日日期），无法直接区分"交易日盘前"与"休市日"。请按以下顺序判断：1）若今天是周六或周日，回复 [SILENT] 并结束；2）若今天是周一至周五，先搜索确认今天是否为法定节假日休市日（如春节、国庆、五一等），若是则回复 [SILENT] 并结束；3）若搜索失败或结果模糊、无法明确确认今日为休市日，一律按交易日正常执行（宁误推不漏推）；4）确认今天为交易日后，调用接口 https://qt.gtimg.cn/q=sz000582 获取行情。注意：若接口请求失败、返回为空或无法解析出价格字段，属于数据源故障而非休市，输出 '⚠️ 数据源异常，请人工检查' 并结束，严禁回复 [SILENT]（接口故障与休市必须可区分，否则监控失效无法被发现）。以上真正的 [SILENT] 情形（仅周末与法定节假日休市），全部输出必须仅为 [SILENT] 这一个词，不生成任何分析内容（系统识别 [SILENT] 后不会推送）。

【行情数据获取】交易日确认后必须调用腾讯财经接口：https://qt.gtimg.cn/q=sz000582，返回数据以~分隔，关键字段：fields[3]=当前价(盘前即上一交易日收盘价), fields[4]=昨收, fields[32]=涨跌幅(%), fields[37]=成交额(万元), fields[30]=快照时间戳。该快照作为「昨日收盘基线」写入输出：个股行情、涨跌幅一律以接口快照为准，不得用搜索到的多日前旧行情（如旧跌停记录）冒充近期走势；历史行情事件如确需引用，必须标注其日期。数据自洽校验：(现价−昨收)/昨收×100 须与涨跌幅（fields[32]）一致（偏差 ≤0.2 个百分点，覆盖四舍五入误差）；不自洽说明字段索引漂移或解析错位，属数据源故障，输出 '⚠️ 数据源异常，请人工检查' 并结束，严禁回复 [SILENT]，不得采用该次数据。

【搜索策略】三层漏斗，确保「平陆运河+任何新事件」不漏报。核心原则：事件关键词永远列不全，但每个事件必有日期——用「主体词×日期」网格兜底：1）逐日网格（必做）：以行情快照日期（fields[30]，即上一交易日）为锚点，对窗口内每个自然日 D（快照日至今天；快照日当天只需15:00收盘后的报道；周一则覆盖周末3天；长假后首个交易日覆盖整个假期），各搜索一次「平陆运河 + D」——D 必须用带年份的完整格式（如 2026年9月17日；严禁只写"9月17日"，否则会命中历史同月日旧闻）；当天任何类型报道（含南宁海关、广西海事局等官方源头）均会命中；2）专项词（必做，防召回不全）：分次搜索「平陆运河 实船试验」「平陆运河 智能示范船」「平陆运河 海关监管」「平陆运河 多式联运」；3）公告（必做）：巨潮资讯网「北部湾港」公告列表页，枚举完备、不依赖搜索词；4）按需补搜：西部陆海新通道/RCEP、BDI指数、航运板块。线索深挖规则：凡简讯出现"X月X日完成/持续推进"等带日期线索，须以该日期（同样带年份）补搜展开，不得一句话带过。每条结果先核对报道/事件日期，按日期新旧取舍，而非搜索排名取舍。

【上下文去重】你的上一次输出会被注入。若其为完整报告，其中已报告且无实质性新进展的事件，今日不得重复展开，必要时一句话带过并标注'前期已报'；若其为 [SILENT]，则无已报告事件，正常整理。版面优先留给增量信息。

信息优先级（按以下顺序整理）：
1）核心信息：【平陆运河】最新进展（事件节点以文末注入的【事件日历】为准——按当前实际日期判断所处阶段：试通航前关注准备进展，试通航期间及之后关注通航实况、过闸量、运营数据，兑现后重点关注货量兑现与「卖事实」反转风险；日历只是锚点，以搜索到的最新官方消息为准）、公司最新公告；
2）重要信息：西部陆海新通道/RCEP政策、BDI指数走势、航运板块表现；
3）补充信息：机构研报评级、主力资金流向。

输出格式：
【昨日收盘】一行：收盘价、涨跌幅、成交额（接口快照数据）。
【核心催化】（不超过2条，每条不超过80字，条首标注事件日期）
【利好因素】（不超过2条，每条不超过80字，条首标注事件日期）
【利空因素】（不超过2条，每条不超过80字，条首标注事件日期）
【风险提示】（不超过2条，每条不超过80字）
最后给出一句话综合判断。

防幻觉要求：每条信息请标注信息来源名称（如东方财富网、巨潮资讯网），严禁编造数据，但不要因此中断任务。你自己推断的观点（如"利好兑现即利空"预期、技术形态判断）不属于有出处的信息条目，不得标注'来源待核实'冒充新闻，只能放入【风险提示】或综合判断。

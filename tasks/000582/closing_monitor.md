请查询北部湾港(000582)最新行情并判断是否推送。

【数据源】必须使用腾讯财经接口获取实时行情：https://qt.gtimg.cn/q=sz000582
返回数据以~分隔，解析规则：fields = text.split('~')，关键字段：fields[3]=当前价, fields[4]=昨收, fields[5]=今开, fields[31]=涨跌额, fields[32]=涨跌幅(%), fields[33]=最高, fields[34]=最低, fields[36]=成交量(手), fields[37]=成交额(万元)。

【判断逻辑】
1）计算较昨收涨跌幅（fields[32]），若绝对值超5%，生成异动报告（包含价格、涨跌幅、成交量变化），如有明显原因请简要说明，无法判断则标注'暂无明显驱动'；
2）涨跌幅在5%以内，回复'[SILENT]'，不推送任何消息；
3）如遇熔断、临时停牌等极端情况，输出'⚠️ 极端行情（熔断/临停），请人工介入'；
4）如接口请求失败或返回异常，输出'⚠️ 行情数据获取失败，请检查数据源配置'。

防幻觉要求：价格、涨跌幅等数值必须来自实时行情接口，严禁编造。
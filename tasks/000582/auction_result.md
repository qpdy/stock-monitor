请查询北部湾港(000582)集合竞价最终结果。

【数据源】必须使用腾讯财经接口获取实时行情：https://qt.gtimg.cn/q=sz000582
返回数据以~分隔，解析规则：fields = text.split('~')，关键字段：fields[3]=当前价, fields[4]=昨收, fields[5]=今开, fields[32]=涨跌幅(%), fields[36]=成交量(手), fields[37]=成交额(万元)。数据自洽校验：(现价−昨收)/昨收×100 须与涨跌幅（fields[32]）一致（偏差 ≤0.2 个百分点，覆盖四舍五入误差，竞价虚拟价同样自洽）；不自洽说明字段索引漂移或解析错位，属数据源异常，按下方【接口故障判断】处理，严禁回复 [SILENT]，不得采用该次数据。

【交易日与快照时效校验】核对外层行情快照的时间戳（fields[30]，格式如20260826092501）：
1）前8位日期不是今天 → 判定为非交易日，全部输出仅回复 [SILENT] 一个词并结束，不生成任何其他内容（系统识别 [SILENT] 后不会推送）；若时间戳字段缺失、为空或前8位无法解析为合法日期，属数据源异常而非休市，按下方【接口故障判断】处理，严禁回复 [SILENT]。
2）日期为今天、但时分早于 09:24 → 读到的是 9:25 竞价撮合完成前的虚拟匹配数据（接口延迟或缓存所致），严禁当作开盘价报出：输出'⚠️ 快照停留在 HH:MM（早于9:25撮合），本次读数可能为撮合前虚拟价，开盘价请以9:30开盘首笔推送为准'并结束。竞价撮合于 9:25 整完成，下限取 09:24 留 1 分钟余量。

【量能参照】为给出承接强弱分型，另调腾讯日K接口取昨日全天成交量：https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sz000582,day,,,2,qfq （qfq 参数不可省略——实测省略时该接口返回空数据；返回 JSON 中 data.sz000582.qfqday 为日K数组，每条为 [日期,开盘,收盘,最高,最低,成交量(手)]，取其中日期非今天的最后一条的成交量，即昨日全天成交量；若返回无 qfqday 键但有 day 键，用 day）。该接口失败、返回为空或取不到昨日数据时，跳过承接分型、只输出竞价结果主体——量能参照是增强信息，不因其失败而告警或阻塞主流程。

【接口故障判断】若行情接口请求失败、返回为空或无法解析出上述字段，属于数据源故障而非休市，输出 '⚠️ 数据源异常，请人工检查' 并结束，严禁回复 [SILENT]（接口故障与休市必须可区分，否则监控失效无法被发现）。

【输出要求】
强制输出最终竞价结果与承接分型，作为全天强弱基准，如'🔔 竞价结束，开盘价X.XX元，涨跌幅X.XX%，竞价成交量X手，约为昨日全天量的Y%，承接定性一句话'。承接定性结合开盘方向与竞价量占比的相对大小（无统一硬阈值）：高开+竞价量占比明显偏大=强承接；高开+占比很小=虚涨（谨防冲高回落）；低开+放量=抛压重；低开+缩量=观望情绪浓。昨日量数据缺失时省略占比与定性，仅报竞价结果。

【备用数据源】腾讯接口本次请求失败或数据自洽校验不通过时，改用东方财富接口 https://push2.eastmoney.com/api/qt/stock/get?secid=0.000582&fields=f43,f46,f60,f170 （f43=最新价,f46=今开,f60=昨收,f170=涨跌幅%，均为放大100倍的整数，需除以100）作应急数据源，并在输出开头标注'⚠️ 主数据源异常，已切换备用源'。备用源无快照时间戳字段，切换后交易日与快照时效校验随之失效，仅按涨跌幅阈值输出竞价结果并标注时效未验证。

防幻觉要求：价格、涨跌幅等数值必须来自实时行情接口，严禁编造。

# AGENTS.md — stock-monitor 会话规约

本仓库是生产监控仓库（腾讯云，hermes 自动部署）。任何会话（微信 gateway / 交互 / cron）在本目录工作时遵守以下规约。

## 铁律

1. **只读仓库**：禁止修改/删除仓库内文件，禁止 git 操作（pull / commit / push），禁止执行部署脚本（deploy.sh / undeploy.sh / scripts/auto_update.sh）。仅允许：读文件、执行 `scripts/` 下的脚本。
2. **数值只来自脚本**：任何行情 / 财务数值必须来自 `scripts/quote.py` 或 `scripts/screen.py` 的 stdout，逐字转述——禁止自行取数、心算估算、补充脚本输出之外的任何数字（含个股横向对比、行业均值、"XX 常年 X%"类记忆数据）。历史教训：2026-08-27 曾发生模型编造自洽数据事故。

## 排雷指令（微信触发）

- 收到「排雷 / 排雷检查 / 排雷报告 + 代码」类请求 → 执行：
  `python3 scripts/screen.py check <代码>`
- 代码一律转成**纯 6 位数字**（如 `600000`）；用户消息带 sh/sz 前缀或其他写法时自动剥掉重跑，**不要反问用户**
- 用户发**中文名或位数不对的代码**（非 6 位数字）→ 回复请用户提供 6 位代码，**禁止自行搜索名称转代码**（名称转码无脚本口径，模型自取数是事故源）
- 多代码请求（如「排雷 600000 000582」）→ 逐个执行、逐个完整转述，不合并摘要
- 转述规则：保留脚本文本报告的结构（检查项 / 数值 / 判定 / 结论行），判定枚举 PASS / FAIL / WARN / MANUAL / NA 原样保留，不做改写
- 允许：解释各项含义；提醒 MANUAL 项须巨潮资讯网（cninfo.com.cn）人工复核
- 禁止：脚本外的数字与对比、买卖建议、**免责声明段**（用户已知悉，勿重复）
- **数据缺失 / MANUAL 时如实呈现缺失原因，禁止用任何其他来源（实时行情、记忆、搜索结果）补全数值**——缺就缺，留给用户人工补查；不要"帮忙拉价格补全"
- 脚本输出 ⚠️ 异常话术时原样转述，自行重试至多 1 次，仍异常即请用户人工检查

## 其他请求

- 与行情 / 财务无关的问题正常回答
- 涉及本仓库任务配置（schedule.json / tasks/ / deploy.sh）的问题：只解释，不修改
